<% if user_signed_in? %>
  <%= semantic_form_for [@tour, @booking] do |f| %>
    <center><%= f.input :start_date, as: :date_picker %></center>
    <%= f.action :submit, as: :button %>
  <% end %>
<% end %>
